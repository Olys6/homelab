# Random path prefix that gates the mealie-mcp endpoint. Generate with: openssl rand -hex 24
MEALIE_MCP_PATH_SECRET=changeme

# Bearer token MCP clients must send to reach mealie-mcp. Generate with: openssl rand -hex 32
MEALIE_MCP_TOKEN=changeme
