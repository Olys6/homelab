# This machine's Tailscale IPv4 (`tailscale ip -4`); the value below is a placeholder
TAILSCALE_IP=100.64.0.1
# Absolute path holding one directory per stack, plus a stacks/ dir of symlinks to them
DOCKER_ROOT=/opt/docker
