# Minimal values for running openGym behind Caddy. The full list of options is in
# upstream's .env.example: https://gitlab.com/DuarteSantos8/opengym
# Passkeys are bound to this exact hostname.
RP_ID=opengym.example.com
ORIGIN=https://opengym.example.com
RP_NAME=openGym
